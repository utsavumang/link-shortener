<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>All Shortened URLs - Stats</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>

  <!-- Include Navbar -->
  <%- include("partials/navbar", { links: navbarLinks }) %>

  <main class="homepage">
    <h1>All Shortened Links - Statistics</h1>

    <% if (urls.length === 0) { %>
      <p>No links shortened yet.</p>
    <% } else { %>
      <table>
        <thead>
          <tr>
            <th>Serial No.</th>
            <th>Original URL</th>
            <th>Short URL</th>
            <th>Clicks</th>
          </tr>
        </thead>
        <tbody>
          <% urls.forEach((url, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><a href="<%= url.redirectURL %>" target="_blank"><%= url.redirectURL %></a></td>
              <td>
                <a href="/url/<%= url.shortId %>" target="_blank">
                  <%= req.protocol + '://' + req.get('host') + '/url/' + url.shortId %>
                </a>
              </td>
              <td><%= url.visitHistory.length %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  </main>

</body>
</html>
